{
  "schemaVersion": 1,
  "type": "eval-fixture",
  "enabled": false,
  "fixture": "gbeth-smoke",
  "description": "Optional GBETH.pdf smoke fixture; disabled by default.",
  "when": {
    "module_type": "ethernet"
  },
  "cases": [
    {
      "id": "fixture-gbeth-smoke-smoke",
      "tool": "hybrid_search_pdf",
      "args": {
        "query": "driver relevant register sequence interrupt reset status",
        "intent": "auto",
        "top_k": 5
      },
      "assertions": {
        "mustContain": [
          "Hybrid search results"
        ],
        "mustNotContain": [
          "Unhandled",
          "TypeError",
          "ReferenceError"
        ]
      },
      "allowFail": true
    }
  ]
}